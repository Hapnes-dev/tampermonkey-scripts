Generate ONE raw IWMAC Designer panel JSON: fixed-canvas catalogue objects; visible text in tag_text, signal description in alias_text. User inserts via Insert JSON and links by hand, unless LINKING applies.

PRECEDENCE: supplied panel JSON/screenshot; same-type production export; panel-type geometry contract; CLAUDE.md; AI-BRIEFING.txt; PANEL-TYPE-GUIDE.md; DESIGN-OBJECT-CATALOG.md; generic advice. Never average conflicts. Production-role coverage, not object count, is target.

OUTPUT. Top: format "iwmac-designer-panel", version 1, generator, source_plant_id, panel_name, panel_width/height with px, counts equal array lengths, background_embedded, panel. panel: plant_id/name/same size, org_image_name, image_name, saved_by "copilot", single_objects, containers (only spjeldliste/room table), graphics []. Vent owns its background.

BACKGROUND, optional, NEVER authored on Ventilasjon. panel.image_svg: svg tag, matching viewBox, no scripts, object pixel space; schematic only, no fake values. PNG: image_data + converted "true". Set background_embedded true.

OBJECT, unlinked template; LINKING/ROMKONTROLL override stated fields. All 17: obj_id, sequential name, id "driver_id", integer posWidth/Height/Left/Top, zIndex "default", tag_text, linked "false", link_name/link_tag/sub_group "", driver_id "driver_id", unit_id/unit_ref "", alias_text.

LINKING; terms owned by AI-BRIEFING.txt 8b. 1 linked="true" is not proof. 2 Non-empty/plausible driver_id is not proof. 3 Resolve every intended link exactly and uniquely in supplied plant parameters. 4 Verify driver_id, unit_id, controller identity, exact alias/meaning, role, access and datatype together. 5 Never construct/transform IDs: no prefix, family, controller, group or suffix edits; no AK2→AK3 or 001:→000: inference. 6 Unmatched/ambiguous/incompatible means UNRESOLVED; fuzzy matches are candidates only. 7 Report intended, structurally linked, source-resolved, semantically verified and unresolved counts separately, plus matrix/evidence. 8 Never say fully linked, linked-ready, production-ready or verified unless every intended link is semantically verified; binding proof alone is not production readiness. 9 Preserve geometry, obj_id, names, order, zIndex and background while repairing. 10 Compare Oversikt by controller+role, never array index/proximity; preserve object_10000 names. BAD: linked true + non-empty driver_id = valid. GOOD: exact unique source row + exact unit/controller + exact meaning matching display/alarm/cooling/defrost role + suitable access/datatype = source-resolved and semantically verified. Any unresolved role is a HARD STOP: deliver verified subset, matrix, source coverage, unresolved roles and missing evidence. Retain unresolved original binding fields and label UNVERIFIED externally; JSON has no verification field.

OBJECT IDS. Never infer from a name or wildcard. Copy each exact obj_id from the supplied panel, panel-type contract or DESIGN-OBJECT-CATALOG.md. Catalogue sizes are toolbox defaults, not placement rules.

LAYOUT, table-style only (7a/7c/7d, ROMKONTROLL override it). x 20-1380, y 10-730 (lists/room tables run past; the view scrolls). Columns x 20/320/640/960: header bar, rows every 28-30px; row = label (x+5, W 100-150) + value (x+150-180). Title top left, bell ~x1240 y6. 8 px gaps on table panels; schematics overlap on purpose, never on TEXT.

TEXT ALIGNMENT. number_v3_label_11px_norm renders LEFT-ALIGNED from posLeft; posWidth does not centre it. Centre on column centre C for rendered width W: posLeft = C; W/2.

MASKIN = CO2 rack. The drawing owns ALL artwork; objects add live values only. Read MASKIN-COPILOT-PREFLIGHT.md; MASKIN-GENERATION-CONTRACT.md owns every coordinate, role and z band. Copy maskin-10229-sanitized.json, NOT generated-maskin-example.json (NEGATIVE). z bands 110/360/375/1000/1100, never "default". Clusters atomic.

OVERSIKT = a store MAP: one cluster ON the case, cold room or freezer it monitors; never a dashboard or a grid. Read OVERSIKT-COPILOT-PREFLIGHT.md; OVERSIKT-GENERATION-CONTRACT.md owns every rule. A supplied export is PRESERVED AND PATCHED, never rebuilt; a PDF or byggeplan may name equipment but NEVER reduces the panel. Coverage read from the source, never forced to 4 per controller. CENTRE THE TEMPERATURE BUBBLE ON ITS EQUIPMENT BOX, never on its label: left x+(bw-w)/2, top y+(bh-h)/2, its OWN size, image measures scaled to panel first; unmeasured box = no coordinate, say so. z 110 value box, 375 circular symbols. Copy oversikt-10113-sanitized.json (evidence, not a target). Verify with validate-oversikt-panel.py, --compare SOURCE CANDIDATE when a source exists, --footprints and --patch-scope value-position on a centering patch.

ROMKONTROLL TABLE ("tabell romkontroll alle plan") = rooms x signals grid; not the 7d floor plan, not 7c. ONE container, container_type "table_container", unique_id must contain custom_ or the host drops it; container zIndex 4, items "5"; num_of_col/num_of_rows/last_y match the grid; taller than the canvas ON PURPOSE; report it, never compress. Dump supplied = link it (LINKING). ROMKONTROLL-GENERATION-CONTRACT.md owns it; check with validate-romkontroll-panel.py.

LINK EXECUTION. Exact lookup first; copy one unique role-compatible source row VERBATIM; id/link_name stay host literals. Maskin uses maskin-akpc-link-map.json. EXISTING OVERSIKT triggers: "link these objects", "link out these disks", "find the objects on this picture", "these need linking", "I placed them where they should be", "use this updated JSON", "match the screenshot to the parameter Excel", "repair missing links on an Oversikt panel". Use newest JSON; inspect image_data then trace; preserve placement; match full labels to workbook; copy exact IDs; add only missing live roles; missing unit STOPs that cluster; array order is not physical order; clusters are not containers; same-cluster stacking is legal; return panel plus unmatched labels. Contract 8.6. Validate with --parameters, --compare, --patch-scope binding-repair, --task oversikt-existing-panel-relink.

HOUSE STYLE. Keep system-numbered panel names. Match plant size: 1400x750/755 or legacy 1280x1024; no V2 ids. MODE A: blank table. MODE B: labelled 90px-pitch cluster-kit HAND-OFF, never finished Oversikt. MODE D: no-export byggeplan draft with image_svg and plan aliases; with export, patch it. MODE C: spjeldliste per briefing 7c. Hotels per 7d: nav-tile hub; rc_* floor plans/tappevann.

VENTILASJON = process schematic + right control sidebar, NOT a dashboard. VENTILATION-GEOMETRY-CONTRACT.md carries every coordinate, scope-tagged; trace it object by object; never invent a vent layout; bg 00-blank-sidebar-1400x750, background_embedded true. Name the case: demo / copy / edit of an attached export (preserve every field) / background-only (vent has none; stop). Copy POSITIONS from the reference, instrument CODES from the target plant; the refs swap RT4xx/RT5xx. zIndex bands per the contract, never mixed with "default". A repeated or decorative alarm is a defect.

VENT OVERLAP is intentional; never "fix" it; the contract lists the five classes. NEVER shorten a duct to clear a damper. The x411 bypass is ONE cluster; no recirculation leg means no column.

BEHAVIOR. Plant number given: source_plant_id + plant_id, else "". Unclear: ask ONE question. Answer = raw JSON only, no fences, prose or comments.

SELF-CHECK: counts = array lengths; obj_ids allowlisted; all 17 fields; driver_id "driver_id" + linked "false" (unless LINKING); zIndex "default" (7c, ROMKONTROLL, vent/maskin bands excepted); integer positions in canvas (lists/room tables may run past); no unintended overlap (VENT OVERLAP excepted); values fit LONGEST allowed value; graphics empty, containers only spjeldliste/one room table; names object_0..N sequential; UTF-8; °C not gr C, keep æøå and m³; vent: no image_svg, sidebar present, clusters whole, headings centred by rendered width, no source ids.
